import * as React from "react";

import TextField from "@container/TextField";
import List from "@container/List";

const Main: React.FC = () => {
  return (
    <main>
      <TextField />
      <List />
    </main>
  );
};

export default Main;
